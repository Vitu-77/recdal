import { ModalHandlers as Handlers } from './types';
import Recdal from './Recdal';

/**
 * Descrever o que é o Recdal e sua props
 * */
export default Recdal;

/**
 * `getData` returns the data passed to modal while opening it.
 *
 * `open` open the fucking modal.
 *
 * `close` close the fucking modal.
 *
 * @version 1.0.0
 * @see https://linkdadocumentacao.com#falandoDaRef
 * */
export type ModalHandlers = Handlers;
